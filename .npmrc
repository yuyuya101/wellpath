fetch-timeout=300000
fetch-retries=5
fetch-retry-maxtimeout=120000
# esbuild 已在 pnpm-workspace.yaml / package.json 白名单；关闭运行前的依赖状态强校验，避免历史 node_modules 的 ignored 状态误阻断脚本
verify-deps-before-run=false
# CI/云端：存在被忽略的构建脚本时只警告、不让 install 以非零退出（白名单已显式放行 esbuild/sharp）
strict-dep-builds=false
