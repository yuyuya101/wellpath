# ADR-06 测试金字塔

- 状态：Accepted（3.1 冻结，P1-8 收敛 E2E 口径）

## 决策
- 底层：Vitest 纯函数单测，健康算法 100% 分支覆盖、服务层 ≥80%。
- 中层：Hono app.request 注入式 API 集成测试，数据库用 PGlite，不起端口。
- 顶层：仅 1 条 Playwright 关键链路冒烟（桌面+移动双视口），不做全量 E2E。

## 后果
反馈快、稳定、不依赖外部服务；真实浏览器只验证最关键的「测评→结果→支付恢复」链路。
